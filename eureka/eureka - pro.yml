server:
  port: 8761
eureka:
  instance:
    hostname: 40.72.153.95
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
spring:
  profiles:
    active: pro
  security:
    basic:
      enabled: true
    user:
      name: zx
      password: zx



